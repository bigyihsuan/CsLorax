# iterative
function factorial(n) {
    let out = 1;
    for i from 1 to n {
        out *= i;
    }
    return out;
}

# recursive
function factorial(n) {
    if (n == 1) {
        return 1;
    }
    return n * factorial(n - 1);
}