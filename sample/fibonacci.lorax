function fibonacci(n) {
    let prev = 1;
    let curr = 1;
    for (let i = 0; i < n; i += 1) {
        let temp = curr;
        let curr = curr + prev;
        prev = curr;
    }
    return curr;
}